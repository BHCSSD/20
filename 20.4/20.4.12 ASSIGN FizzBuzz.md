# 20.4.12 ASSIGN FizzBuzz
```
20.4.12 ASSIGN FizzBuzz
```

You will create a program that plays the "FizzBuzz" game up to a **user-specified limit**. Reminder to make this professional, separate your UI from your **Logic**.

> [!IMPORTANT]
> **The Golden Rule for this Assignment:** > The `main()` function is the only place where `input()` is allowed. Your helper functions (`forFizz` and `whileFizz`) should only receive data through **parameters** and handle the **output**.

---

## **Requirements**

### **1.`main()`**
Your `main()` function will handle all user interaction. 
* **Input Validation:** Ask the user for a limit. Use a `while` loop and `.isdigit()` to keep asking until they provide a valid positive integer.
* **Customization:** Ask the user if they want to use the standard "Fizz" and "Buzz" or if they want to enter their own custom words (e.g., "Boom" and "Pow").
* **Mode Selection:** Ask the user if they want to run the **(F)or** version or the **(W)hile** version.
* **The Call:** Pass the limit and the custom words into your chosen function as **arguments**.

---

### **2. Part 1: `forFizz()`**
* **Parameters:** This function must accept three parameters.
  * For this assignment, you need to use **default values** to help clean your code... hint
* **Logic:** Use a `for` loop and the modulo operator (`%`) to find multiples of 3, 5, or both.
* **Grammar:** * If divisible by 3: Print `{fizz_word}`
    * If divisible by 5: Print `{buzz_word}`
    * If divisible by both: Print `{fizz_word}{buzz_word}` Concatenated

---

### **3. Part 2: `whileFizz()`**
* **Parameters:** Use the same parameters as the `for` version.
   * reminder to use **default values** to help clean your code...
* **Logic:** Implement a `while` loop that starts at 1 and increments until it hits the `limit`.
* **Requirement:** Ensure you manually increment your counter variable to avoid an infinite loop!

---

### **4. Clean Coding & Beefy Extras**
* **Comments:** Explain how the modulo operator `%` works to find a multiple.
* **F-Strings:** Use f-strings for all output. 
* **Concatenation:** Try to structure your `if` statements so that "FizzBuzz" is created by combining the two strings rather than writing a third `print` statement for it.

---

## **Starter Code Structure**
```python

def forFizz():
    # Comments explaining the loop and modulo logic go here
    pass


def whileFizz():
    # Comments explaining counter management go here
    pass


def main():
    # 1. Ask for limit
    # 2. Ask for custom words
    # 3. Ask for version
    # 4. Pass variables into the correct function
    pass

if __name__ == "__main__":
    main()
```

---

## **Rubric**

| **Criteria** | **Excellent** | **Good** | **Needs Improvement** |
| :--- | :--- | :--- | :--- |
| **Separation of Concerns** | `input()` is **only** in `main`. Helpers only use parameters. | One or two `inputs` leaked into helpers. | `input()` is scattered everywhere. |
| **Input Validation** |Properly validates data | Validates but doesn't loop back on error. | No validation (crashes on strings). |
| **Default Parameters** | Functions uses correct syntax. | Parameters used but no default values. | No parameters used (uses globals). |
| **Logic (Modulo)** | Correctly identifies multiples of 3, 5, and 15. | Minor logic errors in the `if` chain. | Incorrect modulo usage. |

