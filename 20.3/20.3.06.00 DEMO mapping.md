# 20.3.06.00 DEMO mapping
```
20.3.06.00 DEMO mapping
```
```js
let layout = {
  trees: [[2, 2], [2, 3], [5, 8], [6, 8]],
  mountains: [[0, 0], [1, 0], [0, 1]],
  roads: [[4, 0], [4, 1], [4, 2], [4, 3], [5, 3], [6, 3]]
};

let cols = 10
let rows =10
let cellSize

function setup() {
  createCanvas(400, 400);
  cellSize = width / cols
  textAlign(CENTER,CENTER)
}

function draw() {
  background(100, 200, 100);
  drawGridLines()

  //step5 draw each category from the object

}

function drawFeature(arr, col, emoji) {
  // We loop through the array of pairs
  for (let i=0; i<arr.length; i++) {
      //step 1 access each X,Y pair


    //step 2 scale it up to our map size

    
    fill(col);
    noStroke();
    //step 3 draw the outline
     
    
    fill(255);
    textSize(20);

  //step 4 input emoji/picures


  }
}

function drawGridLines() {
  stroke(0, 111)
  for (let i=0; i<=cols; i++) {
    line(i*cellSize, 0, i*cellSize, height);
    line(0, i*cellSize, width, i*cellSize);
  }
}
```
