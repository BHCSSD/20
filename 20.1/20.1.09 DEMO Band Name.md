# 20.1.09 DEMO Band Name

> **Outcomes:**
> * **3.2 Use functions that return values:** The search function returns useful data to the caller.
> * **3.3.1 Use parameters to pass data into modules:** Arrays and search values are passed into the function.
> * **3.6 Analyze logic errors.**
> 
> 

---

**Things to focus on:**

* **`linearSearch`**: Your first time seeing a function search through a dataset.
* **Array / List**: Storing multiple items inside a single variable (`allBands`).
* **Return**: Lets you pass variables and information back out of a function.
* **String Methods**: The `analyzeBandName` function teaches key string search tools (`.startsWith`, `.endsWith`, `.includes`, `.indexOf`, `.charAt`).

> [!IMPORTANT]
> **You will be using this style of code later in a full assignment.**

---

```js
//1. LIST aka ARRAY
let allBands= ["The Beatles", "BTS", "ACDC", "Halsey"]

let nameToCheck= ""//empty string as a placeholder
let currentY= 200

function setup() {
  createCanvas(800, 500)
  background(220) //note, this code only works because bg is here

  drawHeaderBox()
  drawResultsBox()
}

function draw() {
  //Empty! messages are drawn when key events trigger
}


//VIEW
function drawHeaderBox() {
  fill(255)
  strokeWeight(2)
  stroke(50)
  rect(20, 20, width - 40, 120)

  fill(0)
  noStroke()
  textSize(20)
  text("Band Name Analyzer", 40, 55)
  textSize(14)
  text("Press 'b' to search for a band name.", 40, 85)
  text("Press 'a' to analyze the band name with String methods.", 40, 110)
}


function drawResultsBox() {
  fill(255)
  strokeWeight(2)
  stroke(50)
  rect(20, 160, width - 40, 310)
}


//Helper function to send a message directly to the screen
function sendMessage(msgText) {
  fill(0)
  noStroke()
  textSize(16)
  text(msgText, 40, currentY)
  currentY += 40 //move down for the next message
}


//2. CONTROLLER
function keyPressed() {
  if (key== 'b'|| key=='B') {
    nameToCheck = prompt("Enter a band name:")
    
    if (nameToCheck) {
      drawResultsBox()//clear previous messages box
      currentY = 200//reset message line height
      
      //Message #1: Search update


      //3. LINEAR SEARCH
      let foundIndex = linearSearch(allBands, nameToCheck)
      
      if (foundIndex !== -1) {
        sendMessage("Result: Band exists in the list!")
      } else {
        sendMessage("That's a Unique name! Press 'a' to analyze.")
      } //end of nested if

    } //end of outer if
    
  } //end of "b"

 
  if ((key === 'a' || key === 'A') && nameToCheck) {
    let score = analyzeBandName(nameToCheck)
    
    //Message #3: String method analysis output
    
  } //end of "a"
}//end of keyPressed


//3. LINEAR SEARCH
function linearSearch(array, searchTerm) {
  for (let i = 0; i < array.length; i++) {
    if (array[i].toLowerCase() === searchTerm.toLowerCase()) {
      return i
    }
  }
  return -1// -1 means your name is not in the list... 
}



//4. STRING METHODS
function analyzeBandName(bandName) {
  let score = 0

  //TODO #1: Check if starts with "The"
  if (  ) {
    score += 1 
  }

  //TODO #2: Check if ends with "s"
  if (  ) {
    score += 5
  }

  //TODO #3: Check if contains "$"
  if (  ) {
    score += 10 
  }

  //TODO #4: Check position with indexOf
  if (  ) {
    score += 20 
  }

  //TODO #5: Check character at specific index
  if (  ) {
    score += 50 
  }

  return score
}//end of analyzeBandName

```
